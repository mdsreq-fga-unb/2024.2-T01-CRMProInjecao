// sections
import { NotFoundView } from '@/sections/error';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'CRM PRO INJEÇÃO Dashboard - Página não encontrada!',
};

export default function NotFoundPage() {
  return <NotFoundView />;
}
